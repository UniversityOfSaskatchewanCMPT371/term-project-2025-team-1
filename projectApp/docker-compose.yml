version: '3.4'

services:
  sampleproject:
    image: 371-t2-2025-image:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 5173:5173